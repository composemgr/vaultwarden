# Vaultwarden - app.env.sample
# Copy to app.env and adjust values before starting

# Timezone
TZ=America/New_York

# Hostname / domain
BASE_HOST_NAME=$HOSTNAME
BASE_DOMAIN_NAME=example.com

# Admin token (protects the /admin panel)
APP_SECRET_KEY=changeme_secret_key_min_32_chars

# Organisation name shown in invites/emails
APP_ORG_NAME=vaultwarden

# Email / SMTP settings
EMAIL_SERVER_HOST=172.17.0.1
EMAIL_SERVER_PORT=587
EMAIL_SERVER_MAIL_FROM=noreply@example.com
